export * from '@/lib/json';
export type LocaleLeafObject = { [key: string]: string };

export const locales = ['en', 'zh-CN'];

export const localeNames: { [key: string]: string } = {
    'en': 'English',
    'zh-CN': '简体中文',
};
